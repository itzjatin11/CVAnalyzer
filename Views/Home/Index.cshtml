<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CV Analyzer Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: #f8f9fa;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .dashboard-container {
            max-width: 900px;
            margin: auto;
            padding: 40px 20px;
            background: white;
            border-radius: 16px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.08);
        }

        .dashboard-logo img {
            max-height: 150px;
            transition: transform 0.3s ease;
        }

            .dashboard-logo img:hover {
                transform: scale(1.05);
            }

        h2 {
            font-weight: 700;
            color: #0d6efd;
        }

        .btn-modern {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            padding: 14px;
            border-radius: 10px;
            font-size: 1rem;
            font-weight: 500;
            background: white;
            border: 2px solid transparent;
            box-shadow: 0 2px 8px rgba(0,0,0,0.05);
            transition: all 0.3s ease;
            text-decoration: none;
        }

            .btn-modern:hover {
                transform: translateY(-3px);
                box-shadow: 0 4px 14px rgba(0,0,0,0.1);
            }

        .btn-primary-modern {
            border-color: #0d6efd;
            color: #0d6efd;
        }

            .btn-primary-modern:hover {
                background: #0d6efd;
                color: white;
            }

        .btn-info-modern {
            border-color: #0dcaf0;
            color: #0dcaf0;
        }

            .btn-info-modern:hover {
                background: #0dcaf0;
                color: white;
            }

        .btn-success-modern {
            border-color: #198754;
            color: #198754;
        }

            .btn-success-modern:hover {
                background: #198754;
                color: white;
            }
    </style>
</head>
<body>

    <div class="container my-5">
        <div class="dashboard-container text-center">
            <div class="dashboard-logo mb-4">
                <img src="image/logo.png" alt="College Logo" class="img-fluid">
            </div>

            <h2 class="mb-2">📊 CV Analyzer Dashboard</h2>
            <p class="text-muted mb-5">Effortlessly manage CV uploads, explore candidates, and analyze skill clusters</p>

            <div class="row g-3">
                <div class="col-12 col-md-4">
                    <a href="/CV/Upload" class="btn-modern btn-primary-modern w-100">
                        📤 Upload CVs
                    </a>
                </div>
                <div class="col-12 col-md-4">
                    <a href="/CV/ViewCandidates" class="btn-modern btn-info-modern w-100">
                        📄 View Candidates
                    </a>
                </div>
                <div class="col-12 col-md-4">
                    <a href="/CV/SkillClusters" class="btn-modern btn-success-modern w-100">
                        🧠 Skill Clusters
                    </a>
                </div>
            </div>
        </div>
    </div>

</body>
</html>
